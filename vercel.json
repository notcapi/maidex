{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "env": {
    "NEXTAUTH_URL": "${VERCEL_URL}"
  },
  "buildCommand": "npm run build",
  "installCommand": "npm install dotenv"
} 