{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "env": {
    "NEXTAUTH_URL": "${VERCEL_URL}",
    "NODE_VERSION": "20.x"
  },
  "buildCommand": "npm run build",
  "installCommand": "npm install dotenv",
  "framework": "nextjs"
} 