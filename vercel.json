{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "env": {
    "NEXTAUTH_URL": "${VERCEL_URL}"
  },
  "buildCommand": "npm install dotenv && npm run build"
} 